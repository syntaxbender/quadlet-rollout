# Grouped SAN certificate example (single cert for multiple domains)
CERT_NAME=example-main
CERT_EMAIL=info@example.com
DOMAINS=example.com subdomain.example.com subdomain2.example.com
# Optional override per bundle. If empty, ACME_CHALLENGE_ROOT from rollout config is used.
# WEBROOT=/var/www/certbot
